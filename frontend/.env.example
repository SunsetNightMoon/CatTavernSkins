# 前端环境变量示例
# 复制为 .env 后按需修改。

# ─── 后端 API 显示地址（用于页面上展示 Yggdrasil 认证 URL 等）───
# 留空则使用 window.location.origin。
VITE_API_URL=http://localhost:3000

# 开发环境 Vite 代理目标（vite.config.ts 读取 VITE_PROXY_TARGET）
VITE_PROXY_API_URL=http://localhost:3000

# ─── 跨站部署：后端 API 基址（EdgeOne Pages 等）───
# 留空 = 同源，前端走相对路径 /api（Windows 一键启动 / Docker 不受影响）。
# 设为后端绝对地址即启用跨站改写（axios + fetch + /uploads 自动指向该地址，并携带跨站 Cookie）。
# 例：VITE_API_BASE=https://api.example.com
VITE_API_BASE=
